# Copy to .env.local and fill values. NEVER commit real tokens to git.
VITE_API_URL=https://api.github.com
# Generate a Personal Access Token (classic) with repo or public_repo scope and set it here
VITE_GITHUB_API_TOKEN=ghp_xxx_REPLACE_ME
